{
  "name": "equialgo-alerts",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:ui": "cd ui && pnpm install && pnpm run build",
    "start": "node dist/index.js",
    "daily-check": "node dist/scripts/daily-check.js",
    "dev": "tsc && node dist/index.js",
    "schwab-login": "node dist/scripts/schwab-login.js",
    "verify:schwab": "node -e \"require('dotenv').config(); require('./dist/trader').verifySchwabConnection(1).then(r => { if (r.ok) { console.log('Schwab API: OK', r.positionsCount !== undefined ? '(' + r.positionsCount + ' positions)' : ''); } else { console.error('Schwab API: FAILED -', r.message); process.exit(1); } }).catch(e => { console.error('Error:', e.message); process.exit(1); })\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^25.0.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "@sudowealth/schwab-api": "^2.1.1",
    "dotenv": "^17.2.3",
    "puppeteer": "^24.36.1",
    "selfsigned": "^5.5.0"
  },
  "packageManager": "pnpm@10.33.0"
}
